#Git Demo repository

This is my first file in Git repository

## this is great learning experience. Basic Git commands

git init
git add
git commit -m

git commit -am -- this is add all the added/modified files and commit the changes to Git repository

git restore --staged file_name -- This will revert the file from staged area

git restore file_name -- this will revert changes from the working directory

## Demo Site content

1. index.html

git status -- this will give add/modified file details and commit status
git restore --filename - this will restore the previous version in working directory